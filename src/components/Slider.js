'use strict';

import React from 'react';

const Slider = React.createClass({
    propTypes: {
    },

    render() {
        return (
            <div>
                Slider
            </div>
        );
    }
});

export default Slider;
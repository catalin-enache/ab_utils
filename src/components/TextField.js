'use strict';

import React from 'react';

const TextField = React.createClass({
    propTypes: {
    },

    render() {
        return (
            <div>
                TextField
            </div>
        );
    }
});

export default TextField;